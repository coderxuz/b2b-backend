meta {
  name: Create Category
  type: http
  seq: 1
}

post {
  url: {{base_url}}/admin/categories
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "name": "Soft Drinks"
  }
}
